import WorkflowRuns from '../src/workflowRuns';

// This is a placeholder test. You should replace it with real tests for your SDK.
test('WorkflowRuns runs without crashing', () => {
  const workflowRuns = new WorkflowRuns('ARGIL_API_KEY');
  expect(workflowRuns).toBeTruthy();
});
