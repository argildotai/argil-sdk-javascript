import Workflows from '../src/workflows';

// This is a placeholder test. You should replace it with real tests for your SDK.
test('Workflows runs without crashing', () => {
  const workflows = new Workflows('ARGIL_API_KEY');
  expect(workflows).toBeTruthy();
});
